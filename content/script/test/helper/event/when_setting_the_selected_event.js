goog.require('goog.events');

goog.provide('src.test.helper.events.whenSettingTheSelectectedEvent');

/**
 @export
 */
src.test.helper.events.whenSettingTheSelectectedEvent.describe = function() {
  //Fields
  
  //Test Hooks
  beforeEach(function() {
    
  });
  
  //Support Methods
  
  //Test Methods
  
  it('should fire when the selection changes', function() {
    //var methodWasCalled = false;
    //var select = src.base.helper.domCreation.select({id: 'ihhihi'}, [{text: 1, value: 2}, {text: 3, value: 4}]);
    //
    //src.base.helper.events.setOnChange(select, function(item) { methodWasCalled = true; });
    //goog.events.Event.call(select, goog.events.EventType.SELECT);
    //
    //expect(methodWasCalled).toBe(true);
    //
    
    expect('This test method').toBe('completed later.');
  });
};

describe('When the onSelect event is set, it', function() {
  src.test.helper.events.whenSettingTheSelectectedEvent.describe();
});
